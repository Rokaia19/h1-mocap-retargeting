# ROS2 colcon build artifacts -- regenerated by `colcon build`, don't commit
build/
install/
log/

# Python
__pycache__/
*.pyc

# Leftover scratch file from debugging, safe to ignore
mujoco_retarget/test_render_tmp.py

# Shared mocap data + URDF-derived assets -- teammate already has these
# (raw mocap CSVs, and the compiled MuJoCo model/meshes are regenerable from
# ros_gz_h1_description via convert_urdf_to_mjcf.py), so don't duplicate them
# in this repo.
mujoco_retarget/data/
mujoco_retarget/model/
src/h1_mocap_retarget/data/*.txt
